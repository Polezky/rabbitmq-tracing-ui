
{mapping, "tracingui.exchange", "rabbitmq_tracingui.exchange",
    [{datatype, string}]}.

{translation, "rabbitmq_tracingui.exchange",
fun(Conf) ->
    Exchange = cuttlefish:conf_get("tracingui.exchange", Conf),
    list_to_binary(Exchange)
end}.